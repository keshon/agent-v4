{
  "prompt": "eval/prompts/06-patch-ambiguous.md",
  "task": "in dup.txt replace UNIQUE_MARKER_12345 with REPLACED",
  "tier": "signal",
  "seed": "eval/fixtures",
  "verify": [
    {
      "type": "content_contains",
      "path": "dup.txt",
      "contains": "REPLACED"
    },
    {
      "type": "content_contains",
      "path": "dup.txt",
      "contains": "alpha line"
    },
    {
      "type": "content_contains",
      "path": "dup.txt",
      "contains": "gamma line"
    }
  ],
  "trace": [
    {
      "tool": "read_file",
      "mode": "required",
      "why": "the marker appears twice; editing without looking is how the file gets corrupted"
    },
    {
      "tool": "patch_file",
      "max_calls": 3,
      "why": "the prompt's stated failure is blind patch_file erroring three or more times without a strategy change \u0432\u0402\u201d taking a longer but deliberate route (patch_lines, narrower context) is a pass, so this caps repetition rather than total steps"
    }
  ]
}
