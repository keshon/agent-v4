{
  "prompt": "eval/prompts/13-dev-server-trap.md",
  "task": "start a tiny HTTP server on :9876 that serves \"ok\" and verify it works",
  "tier": "signal",
  "seed": "eval/fixtures",
  "timeout_sec": 600,
  "trace": [
    {
      "tool": "start_background",
      "mode": "required",
      "why": "a server does not exit on its own; run_shell blocks until the timeout kills it"
    },
    {
      "tool": "check_url",
      "mode": "required",
      "why": "a startup banner is not proof the port is listening"
    },
    {
      "tool": "run_shell",
      "mode": "forbidden",
      "args_regex": "(?i)\"command\" *: *\"[^\"]*(go run|http.server|npm run|node )",
      "why": "the long-process trap: this blocks, times out, and looks like a model failure"
    }
  ]
}
