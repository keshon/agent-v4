You've spent several steps searching (grep_files/list_files/read_file) without making any actual file change yet. Don't keep narrowing the same regex or re-checking directories you've already listed. Try a genuinely different approach: drop glob filters and search a single simple keyword instead of a multi-term pattern, check file types you haven't tried yet (templates, HTML, JS, not just one language), or list directories you haven't explored. If you still can't locate what you need, use ask_user to ask the user directly rather than continuing to search indefinitely.
